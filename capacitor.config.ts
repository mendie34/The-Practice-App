import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thepracticeapp.golf',
  appName: 'The Practice App',
  webDir: 'dist'
};

export default config;
